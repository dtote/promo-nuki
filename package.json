{
  "name": "promo-nuki",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve src/index.html",
    "build": "rm -rf build && parcel build src/index.html -d build --public-url /new-project/",
    "min": "npx postcss src/css/index.css -u postcss-clean -o src/css/index.min.css --no-map",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.20.0",
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "postcss-clean": "^1.2.2",
    "postcss-cli": "^8.3.1",
    "stylelint": "^13.10.0",
    "stylelint-config-standard": "^20.0.0"
  }
}